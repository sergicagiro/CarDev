#include <stdio.h>

int main() {
  int x = 5;
  int *ptr = &x;
  
  printf("x = %d\n", x);
  printf("ptr = %p\n", ptr); 
  printf("*ptr = %d\n", *ptr);

  *ptr = 10;
  
  printf("x = %d\n", x);
  printf("*ptr = %d\n", *ptr);

  return 0;
}